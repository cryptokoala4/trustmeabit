app.controller('WalletCtrl', ['$http', '$scope', '$auth', '$location',
  function($http, $scope, $auth, $location){

      $http
        .get('http://localhost:3000/api/show')
        .then(function(response){
          $scope.balance = response.data.balance / 100000000;
          // $scope.addresses = response.data.address[0].address;
          // $scope.addresses = response.data.address;
          // console.log(response.data)//[0].balance;
    });
   }
])

          // $scope.balance = response.data.address[0].balance / 100000000;


